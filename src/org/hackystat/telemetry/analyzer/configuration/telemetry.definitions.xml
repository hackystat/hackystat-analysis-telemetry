<TelemetryDefinitions>

<TelemetryDefinition DefinitionType="Streams" ShareScope="Global">
<![CDATA[
streams ActiveTime(filePattern, cumulative) = {
  "Active Time for this Project", 
  ActiveTime(filePattern, cumulative)
};
]]>
</TelemetryDefinition>   

<TelemetryDefinition DefinitionType="Chart" ShareScope="Global">
<![CDATA[
chart ActiveTime-DevTime-Chart(filePattern, cumulative) = {
  "Active Time v.s. Dev Time", 
  (ActiveTime(filePattern, cumulative), yAxis("hours")),
  (DevTime(filePattern, cumulative), yAxis("hours"))
};
]]>
</TelemetryDefinition>   

<TelemetryDefinition DefinitionType="Report" ShareScope="Global">
<![CDATA[
report DevelopmentProgressReport() = {
  "Active Time, Builds, FileMetrics, and Java Coverage", 
  ActiveTime-Chart("**", "true"), 
  Build-Chart("true"), 
  FileMetric-Chart("totalLines", "**"), 
  Coverage-Percentage-Chart("**", "line")
};
]]>
</TelemetryDefinition>   

<TelemetryDefinition DefinitionType="YAxis" ShareScope="Global">
<![CDATA[
y-axis percentageYAxis(label) = {
  label, "integer", 0, 100
};
]]>
</TelemetryDefinition>         

</TelemetryDefinitions>