<project name="jar" default="publish" xmlns:ivy="antlib:org.apache.ivy.ant">
  <description>
  Provides the target for building a jar file of this service.
  </description>

  <import file="build.xml"/>
  <property name="tmp.dir" location="${basedir}/tmp" />
  <property name="tmp.lib.dir" location="${basedir}/tmp-lib" />
  <property name="build.jar.dir" location="${build.dir}/jar" />
 
  <target name="jar" depends="jar-standalone, jar-lib">
    <!-- Temporary copy to ${basedir} to support packages still using HACKYSTAT_TELEMETRY_HOME. -->
    <copy todir="${basedir}">
      <fileset dir="${build.jar.dir}">
        <include name="*.jar" />
      </fileset>
    </copy>
  </target>

  <target name="jar-standalone" depends="compile" 
    description="Create telemetry.jar">
    <!-- Define the directories and distribution name -->
    <mkdir dir="${tmp.dir}" />
    <mkdir dir="${build.jar.dir}" />
    <copy todir="${tmp.dir}">
      <fileset dir="${basedir}/build/classes"/>
    </copy>
  
    <!-- Build telemetry.jar --> 
    <unjar src="${lib.dir}/hackystat-sensorbase-uh/sensorbaseclient.jar" dest="${tmp.dir}"/>
    <unjar src="${lib.dir}/hackystat-analysis-dailyprojectdata/dailyprojectdata.lib.jar" dest="${tmp.dir}"/>
 
    <jar destfile="${build.jar.dir}/telemetry.jar" basedir="${tmp.dir}">
      <manifest>
        <attribute name="Main-Class" value="org.hackystat.telemetry.service.server.Server"/>
        <attribute name="Implementation-Title" value="Hackystat Telemetry"/>
        <attribute name="Implementation-Version" value="${version}"/>
        <attribute name="Implementation-Vendor" value="Collaborative Software Development Laboratory, University of Hawaii"/>
      </manifest>
    </jar>
    <delete dir="${tmp.dir}" />
  </target> 
  
  <target name="jar-lib" depends="compile" 
     description="Create telemetry.lib.jar, containing just org.hackystat.telemetry code.">
     <!-- Define the directories and distribution name -->
     <mkdir dir="${tmp.lib.dir}" />
     <mkdir dir="${build.jar.dir}" />
     <copy todir="${tmp.lib.dir}">
       <fileset dir="${basedir}/build/classes"/>
     </copy>
   
     <jar destfile="${build.jar.dir}/telemetry.lib.jar" basedir="${tmp.lib.dir}">
       <manifest>
         <attribute name="Implementation-Title" value="Hackystat Telemetry"/>
         <attribute name="Implementation-Version" value="${version}"/>
         <attribute name="Implementation-Vendor" value="Collaborative Software Development Laboratory, University of Hawaii"/>
       </manifest>
     </jar>
     <delete dir="${tmp.lib.dir}" />
   </target> 
  
  <target name="publish" depends="jar" description="Publish this project in the local ivy repository">
    <ivy:resolve />
    <tstamp>
      <format property="publication-revision" pattern="yyyy.MM.dd.hh.mm.ss" />
    </tstamp>
    <ivy:publish artifactspattern="${build.jar.dir}/[artifact].[ext]" resolver="local-repository" pubrevision="${publication-revision}" update="true" />
  </target>
</project>

